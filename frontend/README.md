# Frontend

See the [project README](../README.md) for full setup, run, and
architecture documentation.

Quick reference, run from this directory:

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
```

Requires `.env.local` (copy from `.env.example`) with the backend's URL —
see the project README's Environment Variables section.
